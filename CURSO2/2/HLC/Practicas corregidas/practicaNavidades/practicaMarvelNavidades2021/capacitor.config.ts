import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.practica.app',
  appName: 'practicaNavidades2021',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
