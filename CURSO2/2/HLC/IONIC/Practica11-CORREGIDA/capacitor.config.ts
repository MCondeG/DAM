import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Practica11-CORREGIDA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
