import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'P17-Evaluable',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
