import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ApiREST_Peliculas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
