import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Practica7_ManuelConde',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
