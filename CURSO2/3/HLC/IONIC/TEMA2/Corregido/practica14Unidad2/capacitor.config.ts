import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'practica14Unidad2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
