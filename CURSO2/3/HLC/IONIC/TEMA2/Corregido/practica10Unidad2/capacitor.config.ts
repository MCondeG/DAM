import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'practica10Unidad2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
