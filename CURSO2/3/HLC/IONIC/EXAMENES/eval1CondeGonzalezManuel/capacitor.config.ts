import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'eval1CondeGonzalezManuel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
